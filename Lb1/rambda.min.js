const stringArray = ['Apple', 'Banana', 'Cherry', 'Date'];

const addPrefix = prefix => string => prefix + string;
const addExclamationPrefix = addPrefix('Wow, ');

const transformedArray = stringArray.map(addExclamationPrefix);

console.log(transformedArray);

function impureGetArrayLength(arr) {
    console.log('Calculating array length...');
    return arr.length;
  }
  
  const array = [1, 2, 3, 4, 5];
  console.log(impureGetArrayLength(array));

  function pureGetArrayLength(arr) {
    return arr.length;
  }
  
  console.log(pureGetArrayLength(array));

  